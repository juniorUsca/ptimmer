import React from 'react'
import { Button } from '@material-ui/core'

const ButtonComponent = (): React.ReactElement => (
  <Button variant="contained" color="primary">
    Hello World
  </Button>
)

export default ButtonComponent
